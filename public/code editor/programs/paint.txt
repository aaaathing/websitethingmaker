{"url":"paint.txt","name":"Paint","type":"html"}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Paint</title>
        <style>
          body, html{
            margin:0;
            padding:0;
            height:100%;
          }
          #paintContainer{
            position:relative;
            height:100%;
          }
          #paintPlace{
            background:transparent;
          }
          #paintBG, #paintPlace{
            width:100%;
            height:100%;
            position:absolute;
          }
        </style>
    </head>
    <body>
      
      <div id="paintContainer">
        <svg xmlns="http://www.w3.org/2000/svg" id="paintBG">
        <pattern id="pattern-checkers" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse">
          <rect fill="#eee" x="0" width="10" height="10" y="0"></rect> 
          <rect fill="#eee" x="10" width="10" height="10" y="10"></rect> 
        </pattern>
        <rect x="0" y="0" width="100%" height="100%" fill="url(#pattern-checkers)"></rect>
      </svg>
        <svg id="paintPlace" xmlns="http://www.w3.org/2000/svg"></svg>
      </div>
      
      <script>
        var paintPlace = document.getElementById("paintPlace");
        var mouseDn = false
        var currentShape
        var svgns = "http://www.w3.org/2000/svg"
        paintPlace.onmousedown = function(e){
          mouseDn = true
          
          currentShape = document.createElementNS(svgns,"rect")
          currentShape.setAttribute("x", e.clientX);
          currentShape.setAttribute("y", e.clientY);
          currentShape.setAttribute("width", "10");
          currentShape.setAttribute("height", "10");
          currentShape.setAttribute("fill", "#5cceee");
          paintPlace.appendChild(currentShape)
        };
        paintPlace.onmouseup = function(){
          mouseDn = false
        };
        
        paintPlace.onmousemove=function(e){
          if(mouseDn && currentShape){
            if(e.clientX > parseInt(currentShape.getAttribute("x")) ){
              currentShape.setAttribute("width", e.clientX - parseInt(currentShape.getAttribute("x")) )
            }else{
              
            }
            if(e.clientY > parseInt(currentShape.getAttribute("y")) ){
              currentShape.setAttribute("height", e.clientY - parseInt(currentShape.getAttribute("y")) )
            }else{
              
            }
          }
        }
        
      </script>
    </body>
</html>